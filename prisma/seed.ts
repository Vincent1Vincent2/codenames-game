/* async function main() {
  for (const card of substantivArray) {
    await db.card.create({
      data: card,
    });
  }
}

main()
  .catch((error) => {
    console.error("Error seeding data:", error);
  })
  .finally(async () => {
    await db.$disconnect();
  });
 */
